v14 exact-object import transport branch

This isolated branch contains only hash-bound transport material and one workflow.
It is not a product candidate and never updates the PR source ref by itself.
