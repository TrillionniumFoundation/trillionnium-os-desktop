name: v13-source-object-import
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'placeholder; no source refs updated'
