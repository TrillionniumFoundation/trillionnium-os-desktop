# Exact-head and live prospective-merge qualification for the sealed S06 BrowserActor boundary.
name: s06-browser-actor

on:
  pull_request:
    paths:
      - ".github/workflows/s06-browser-actor.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/hepta-browser-actor/**"
      - "crates/hepta-browser-actor-simulation/**"
      - "crates/hepta-agent-port/**"
      - "crates/hepta-peer-attestation/**"
      - "crates/hepta-session-core/**"
      - "contracts/browser-actor.v1.json"
      - "contracts/engine-thread-dispatch.v1.json"
      - "contracts/event-loop-completion.v1.json"
      - "contracts/session-incarnation.v1.json"
      - "docs/source-state.v1.json"
      - "docs/architecture/BROWSER_ACTOR_AUTHORITY_BOUNDARY.md"
      - "docs/architecture/ENGINE_THREAD_DISPATCH.md"
      - "docs/architecture/EVENT_LOOP_COMPLETION.md"
      - "docs/architecture/SESSION_INCARNATION.md"
      - "tests/test_s06_browser_actor.py"
      - "tests/test_s06_contract_paths.py"
      - "tools/validate_repository.py"
      - "tools/_validate_repository_impl.py"
      - "tools/validate_project_truth.py"
  push:
    branches: [main, "codex/**"]
    paths:
      - ".github/workflows/s06-browser-actor.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/hepta-browser-actor/**"
      - "crates/hepta-browser-actor-simulation/**"
      - "crates/hepta-agent-port/**"
      - "crates/hepta-peer-attestation/**"
      - "crates/hepta-session-core/**"
      - "contracts/browser-actor.v1.json"
      - "contracts/engine-thread-dispatch.v1.json"
      - "contracts/event-loop-completion.v1.json"
      - "contracts/session-incarnation.v1.json"
      - "docs/source-state.v1.json"
      - "docs/architecture/BROWSER_ACTOR_AUTHORITY_BOUNDARY.md"
      - "docs/architecture/ENGINE_THREAD_DISPATCH.md"
      - "docs/architecture/EVENT_LOOP_COMPLETION.md"
      - "docs/architecture/SESSION_INCARNATION.md"
      - "tests/test_s06_browser_actor.py"
      - "tests/test_s06_contract_paths.py"
      - "tools/validate_repository.py"
      - "tools/_validate_repository_impl.py"
      - "tools/validate_project_truth.py"

permissions:
  contents: read

concurrency:
  group: s06-browser-actor-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  exact-source:
    name: exact-source-authority-boundary
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      EXPECTED_SOURCE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Check out exact source head
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 2
          persist-credentials: false

      - name: Assert exact source identity
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_SOURCE_SHA"
          subject_tree="$(git rev-parse 'HEAD^{tree}')"
          test -n "$subject_tree"
          printf 'evidence_class=exact_head\nsubject_sha=%s\nsubject_tree=%s\n' \
            "$EXPECTED_SOURCE_SHA" "$subject_tree" >> "$GITHUB_STEP_SUMMARY"

      - name: Install exact Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.93.0 --profile minimal --component rustfmt,clippy
          rustup override set 1.93.0

      - name: Validate repository truth, contract paths, and authority boundary
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/validate_repository.py
          python3 tools/validate_project_truth.py
          python3 -m unittest \
            tests.test_s06_browser_actor \
            tests.test_s06_contract_paths \
            -v

      - name: Run complete locked source graph
        shell: bash
        run: |
          set -euo pipefail
          cargo fmt --all --check
          cargo check --workspace --all-targets --all-features --locked
          cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
          listed="$(cargo test --locked -p hepta-browser-actor-simulation --lib -- --list 2>/dev/null | grep -c ': test$' || true)"
          test "$listed" -gt 0
          printf 'actor_simulation_lib_tests=%s\n' "$listed" >> "$GITHUB_STEP_SUMMARY"
          cargo test --locked -p hepta-browser-actor-simulation --lib -- --nocapture
          cargo test --locked -p hepta-browser-actor --doc
          cargo test --workspace --all-targets --all-features --locked

      - name: Reconfirm sealed product API and claim ceiling
        shell: bash
        run: |
          set -euo pipefail
          ! grep -R -n -E 'pub struct BrowserActor<|impl<.*BrowserActor|runtime: R|pub fn new\(|pub fn handle\(' crates/hepta-browser-actor/src
          ! grep -R -n -E 'pub (use|type).*PrincipalBinding|pub (use|type).*MechanismIdentity|PageRuntime,|RequestControl,|RuntimeReply,' crates/hepta-browser-actor/src
          grep -R -n 'pub fn from_attested' crates/hepta-browser-actor/src
          grep -R -n 'pub fn handle_attested' crates/hepta-browser-actor/src
          python3 - <<'PY'
          import json
          from pathlib import Path

          def reject_duplicate(pairs):
              output = {}
              for key, value in pairs:
                  if key in output:
                      raise ValueError(key)
                  output[key] = value
              return output

          def reject_constant(value):
              raise ValueError(value)

          contract = json.loads(
              Path('contracts/browser-actor.v1.json').read_text(encoding='utf-8'),
              object_pairs_hook=reject_duplicate,
              parse_constant=reject_constant,
          )
          authority = contract['authority_boundary']
          assert authority['product_actor_generic_over_runtime'] is False
          assert authority['caller_supplied_runtime_adapter'] is False
          assert authority['runtime_trait_publicly_exported'] is False
          assert authority['request_control_publicly_exported'] is False
          assert authority['deferred_work_after_terminal_success_representable'] is False
          assert contract['browser_actor']['agent_navigation_requires_idle_control_before_runtime'] is True
          assert contract['browser_actor']['terminal_revision_reference_is_never_current'] is True
          assert contract['activation']['product_agent_port_enabled'] is False
          assert contract['activation']['servo_adapter_included'] is False
          assert contract['activation']['production_release_authorized'] is False
          PY

  prospective-merge:
    name: prospective-merge-authority-boundary
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out live prospective merge
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: refs/pull/${{ github.event.pull_request.number }}/merge
          fetch-depth: 2
          persist-credentials: false

      - name: Bind merge parents to live remote refs
        shell: bash
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          git check-ref-format --branch "$BASE_REF"
          test "$PR_NUMBER" -gt 0
          test "$(git rev-parse HEAD)" = "${{ github.sha }}"
          test "$(git rev-list --parents -n 1 HEAD | awk '{print NF}')" -eq 3
          live_base="$(git ls-remote --exit-code origin "refs/heads/$BASE_REF" | awk 'NR == 1 {print $1}')"
          live_head="$(git ls-remote --exit-code origin "refs/pull/$PR_NUMBER/head" | awk 'NR == 1 {print $1}')"
          test -n "$live_base"
          test -n "$live_head"
          test "$live_head" = "$EVENT_HEAD"
          test "$(git rev-parse HEAD^1)" = "$live_base"
          test "$(git rev-parse HEAD^2)" = "$live_head"
          merge_tree="$(git rev-parse 'HEAD^{tree}')"
          printf 'evidence_class=prospective_merge\nsubject_sha=%s\nsubject_tree=%s\nbase_ref=%s\nbase_sha=%s\nhead_sha=%s\n' \
            "$(git rev-parse HEAD)" "$merge_tree" "$BASE_REF" "$live_base" "$live_head" \
            >> "$GITHUB_STEP_SUMMARY"

      - name: Install exact Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.93.0 --profile minimal --component rustfmt,clippy
          rustup override set 1.93.0

      - name: Validate repository truth, contract paths, and authority boundary
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/validate_repository.py
          python3 tools/validate_project_truth.py
          python3 -m unittest \
            tests.test_s06_browser_actor \
            tests.test_s06_contract_paths \
            -v

      - name: Run complete locked merged graph
        shell: bash
        run: |
          set -euo pipefail
          cargo fmt --all --check
          cargo check --workspace --all-targets --all-features --locked
          cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
          listed="$(cargo test --locked -p hepta-browser-actor-simulation --lib -- --list 2>/dev/null | grep -c ': test$' || true)"
          test "$listed" -gt 0
          printf 'actor_simulation_lib_tests=%s\n' "$listed" >> "$GITHUB_STEP_SUMMARY"
          cargo test --locked -p hepta-browser-actor-simulation --lib -- --nocapture
          cargo test --locked -p hepta-browser-actor --doc
          cargo test --workspace --all-targets --all-features --locked

      - name: Reconfirm sealed product API and claim ceiling
        shell: bash
        run: |
          set -euo pipefail
          ! grep -R -n -E 'pub struct BrowserActor<|impl<.*BrowserActor|runtime: R|pub fn new\(|pub fn handle\(' crates/hepta-browser-actor/src
          ! grep -R -n -E 'pub (use|type).*PrincipalBinding|pub (use|type).*MechanismIdentity|PageRuntime,|RequestControl,|RuntimeReply,' crates/hepta-browser-actor/src
          grep -R -n 'pub fn from_attested' crates/hepta-browser-actor/src
          grep -R -n 'pub fn handle_attested' crates/hepta-browser-actor/src
          python3 - <<'PY'
          import json
          from pathlib import Path

          def reject_duplicate(pairs):
              output = {}
              for key, value in pairs:
                  if key in output:
                      raise ValueError(key)
                  output[key] = value
              return output

          def reject_constant(value):
              raise ValueError(value)

          contract = json.loads(
              Path('contracts/browser-actor.v1.json').read_text(encoding='utf-8'),
              object_pairs_hook=reject_duplicate,
              parse_constant=reject_constant,
          )
          authority = contract['authority_boundary']
          assert authority['product_actor_generic_over_runtime'] is False
          assert authority['caller_supplied_runtime_adapter'] is False
          assert authority['runtime_trait_publicly_exported'] is False
          assert authority['request_control_publicly_exported'] is False
          assert authority['deferred_work_after_terminal_success_representable'] is False
          assert contract['browser_actor']['agent_navigation_requires_idle_control_before_runtime'] is True
          assert contract['browser_actor']['terminal_revision_reference_is_never_current'] is True
          assert contract['activation']['product_agent_port_enabled'] is False
          assert contract['activation']['servo_adapter_included'] is False
          assert contract['activation']['production_release_authorized'] is False
          PY
