# Test tree

Future integration suites are grouped as deterministic fixtures, protocol/fuzz,
Servo embedding, QEMU boot, fault injection, network egress, signed-app/origin,
update/recovery, and fixed-hardware qualification. Pure Rust tests currently
live beside their crates.
