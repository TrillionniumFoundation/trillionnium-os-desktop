# D0C-04 final gate

Only exact-head Rust 1.93.0 execution can advance this source candidate. The
next patch after a failing gate must repair the failure and rerun the full gate;
it must not weaken the contract, reduce Clippy severity, remove negative tests,
or enable runtime authority.
