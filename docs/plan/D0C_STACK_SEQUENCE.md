# D0C stacked development sequence

```text
main / D5 foundation
  -> D0C-02 authenticated bounded connected carrier
  -> D0C-03 strict canonical Browser API codec
  -> D0C-04 exactly-one connected AgentPort
  -> exact Rust 1.93.0 promotion evidence
  -> D0C-05 default-disabled socket/service custody
  -> D3 BrowserActor conversion on deterministic local fixtures
```

No listener work may be merged ahead of the exact-head carrier/codec/AgentPort
checks. No BrowserActor or Servo result may be simulated by the D0 mechanism
fixture. External mutation remains closed until controlled egress and effect
reconciliation are separately qualified.
