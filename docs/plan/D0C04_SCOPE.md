# D0C-04 scope lock

D0C-04 ends at an exactly-one, request-bound, connected-stream typed handler
boundary. Filesystem socket custody, service identity, BrowserActor conversion,
Servo runtime integration and every external effect are outside this work
package.
