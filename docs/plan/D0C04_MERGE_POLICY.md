# D0C-04 merge policy

The pull request remains Draft until the exact Rust 1.93.0 job receives a runner
and passes formatting, Clippy, workspace tests, codec tests, AgentPort tests and
the browserd self-check. Source/reference evidence cannot waive that gate.
