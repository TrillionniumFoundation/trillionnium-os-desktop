# D0C-04 runtime ceiling

The source candidate may authenticate and decode a connected request, invoke a
mechanism fixture and bind a response. It may not create a product endpoint,
perform BrowserActor work, call Servo or represent a browser-dependent action
as successful.
