# The pathname parent is root-owned by a client-only traversal group. Preserve
# the historical socket inode identity while keeping it unreachable by pathname
# from the browser mechanism UID.
[Socket]
SocketUser=hepta-browserd
SocketGroup=hepta-agent
SocketMode=0660
DirectoryMode=0750
