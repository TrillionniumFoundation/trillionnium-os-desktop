# Development activation retains the same root-owned pathname custody as the
# product socket. Only the dedicated Agent client group may traverse/connect.
[Socket]
SocketUser=root
SocketGroup=hepta-agent-socket
SocketMode=0660
DirectoryMode=0750
