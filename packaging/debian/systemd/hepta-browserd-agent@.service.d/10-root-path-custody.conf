# The accepted descriptor is inherited from systemd. The connection process
# must not mutate or replace the listener pathname.
[Service]
SupplementaryGroups=
ReadWritePaths=
ReadOnlyPaths=/run/hepta/browserd
