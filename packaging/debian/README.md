# Debian image packaging

D1 image recipes will live here after
`manifests/debian-base.selection.json` is promoted from a base selection to a
resolved signed snapshot lock containing the snapshot timestamp, `InRelease`
digests, signing-key fingerprints, and package-set digest.

No bootable-image claim is made from this placeholder.
