# D1 qualification image only. The production unit remains bound to
# /usr/libexec/hepta-agent-portd outside this image overlay.
[Service]
ExecStart=
ExecStart=/usr/libexec/hepta-agent-d1-fixture --mode server
